---
wave: 1
depends_on: []
files_modified:
  - lib/core/widgets/language_tile.dart
  - lib/features/admin/presentation/screens/admin_home_screen.dart
  - lib/features/admin/presentation/screens/admin_partners_screen.dart
  - lib/features/admin/presentation/screens/admin_sites_screen.dart
  - lib/features/admin/presentation/screens/admin_transactions_screen.dart
  - lib/features/admin/presentation/screens/admin_profile_screen.dart
  - lib/features/admin/presentation/screens/add_edit_site_screen.dart
  - lib/features/admin/presentation/shell/admin_shell.dart
  - lib/features/partner/presentation/screens/partner_home_screen.dart
  - lib/features/partner/presentation/screens/partner_profile_screen.dart
  - lib/features/partner/presentation/screens/partner_transactions_screen.dart
  - lib/features/partner/presentation/screens/add_transaction_screen.dart
  - lib/features/partner/presentation/screens/transaction_detail_screen.dart
  - lib/features/partner/presentation/shell/partner_shell.dart
  - lib/features/super_admin/presentation/screens/add_edit_user_screen.dart
autonomous: true
---

# Plan 10-01: Fix All flutter analyze Warnings

## Goal

Resolve all 125 `flutter analyze` issues (warnings and infos). Zero errors currently exist. After fixes, `flutter analyze --no-fatal-infos` must exit 0. All 33 tests must still pass.

## Tasks

### Task 1: Run dart fix --apply to auto-fix simple issues

<read_first>
- analyze_output.txt (full list of issues)
</read_first>

<action>
Run the following command to auto-fix unnecessary_non_null_assertion and prefer_const_constructors automatically:

```bash
dart fix --apply
```

After running, re-run flutter analyze to see what remains.
</action>

<acceptance_criteria>
- Command `dart fix --apply` runs without crash
- `flutter analyze` issue count decreases after running
</acceptance_criteria>

---

### Task 2: Fix deprecated withOpacity usages

Replace `.withOpacity(x)` with `.withValues(alpha: x)` in:
- `lib/features/partner/presentation/screens/partner_home_screen.dart` lines: 138, 153, 168, 382
- `lib/features/partner/presentation/screens/partner_profile_screen.dart` lines: 49, 69, 72
- `lib/features/partner/presentation/screens/partner_transactions_screen.dart` lines: 269, 308
- `lib/features/partner/presentation/screens/add_transaction_screen.dart` lines: 345, 413

<read_first>
- lib/features/partner/presentation/screens/partner_home_screen.dart
- lib/features/partner/presentation/screens/partner_profile_screen.dart
- lib/features/partner/presentation/screens/partner_transactions_screen.dart
- lib/features/partner/presentation/screens/add_transaction_screen.dart
</read_first>

<action>
For EVERY occurrence of `.withOpacity(x)` in the files listed above, replace with `.withValues(alpha: x)`.

Example transformation:
- BEFORE: `AppColors.primary.withOpacity(0.1)`
- AFTER: `AppColors.primary.withValues(alpha: 0.1)`

Read each file, find all `.withOpacity(` occurrences, and replace all with `.withValues(alpha: `.
</action>

<acceptance_criteria>
- `Select-String -Pattern "withOpacity" lib/features/partner/presentation/screens/partner_home_screen.dart` returns no matches
- `Select-String -Pattern "withOpacity" lib/features/partner/presentation/screens/partner_profile_screen.dart` returns no matches
- `Select-String -Pattern "withOpacity" lib/features/partner/presentation/screens/partner_transactions_screen.dart` returns no matches
- `Select-String -Pattern "withOpacity" lib/features/partner/presentation/screens/add_transaction_screen.dart` returns no matches
</acceptance_criteria>

---

### Task 3: Fix deprecated activeColor in Switch widgets

<read_first>
- lib/features/admin/presentation/screens/add_edit_site_screen.dart (line ~156)
- lib/features/super_admin/presentation/screens/add_edit_user_screen.dart (line ~215)
</read_first>

<action>
In both files, find `Switch` widgets using `activeColor:` and replace with `activeThumbColor:`.

Example:
- BEFORE: `activeColor: AppColors.primary,`
- AFTER: `activeThumbColor: AppColors.primary,`
</action>

<acceptance_criteria>
- `Select-String -Pattern "activeColor" lib/features/admin/presentation/screens/add_edit_site_screen.dart` returns no matches
- `Select-String -Pattern "activeColor" lib/features/super_admin/presentation/screens/add_edit_user_screen.dart` returns no matches
</acceptance_criteria>

---

### Task 4: Fix dead_null_aware_expression and use_build_context_synchronously

<read_first>
- lib/features/admin/presentation/screens/admin_transactions_screen.dart (line 300)
- lib/features/partner/presentation/screens/add_transaction_screen.dart (lines 105, 108)
</read_first>

<action>
**admin_transactions_screen.dart line 300:**
- Issue: `dead_null_aware_expression` — the left operand can't be null so the `??` right operand is never executed
- Fix: Remove the `?? ''` or `?? something` that's dead — just use the non-nullable value directly

**add_transaction_screen.dart lines 105, 108:**
- Issue: `use_build_context_synchronously` — using BuildContext after an async gap
- Fix: Check `if (!mounted) return;` before using context after the async call, OR store `ScaffoldMessenger.of(context)` in a local variable BEFORE the await:
  ```dart
  final messenger = ScaffoldMessenger.of(context);
  final navigator = Navigator.of(context);
  // ... await ...
  messenger.showSnackBar(...);
  navigator.pop();
  ```
</action>

<acceptance_criteria>
- `flutter analyze lib/features/admin/presentation/screens/admin_transactions_screen.dart` shows no `dead_null_aware_expression` warning
- `flutter analyze lib/features/partner/presentation/screens/add_transaction_screen.dart` shows no `use_build_context_synchronously` info
</acceptance_criteria>

---

### Task 5: Fix deprecated value→initialValue in DropdownButtonFormField

<read_first>
- lib/features/partner/presentation/screens/add_transaction_screen.dart (line ~228)
</read_first>

<action>
Find `DropdownButtonFormField` or `FormField` using `value:` that is deprecated in favor of `initialValue:`.

The warning says: `'value' is deprecated and shouldn't be used. Use initialValue instead.`

If the parameter is being used as `value:` on a `FormField` directly (not a `DropdownButtonFormField`), change it to `initialValue:`. If it's on a `DropdownButtonFormField`, `value:` is the correct parameter — check the exact location and determine the fix based on context.
</action>

<acceptance_criteria>
- `flutter analyze lib/features/partner/presentation/screens/add_transaction_screen.dart` shows no `deprecated_member_use` for `value` parameter
</acceptance_criteria>

---

### Task 6: Verify tests still pass

<read_first>
- test/core/currency_formatter_test.dart
- test/core/date_formatter_test.dart
- test/widget_test.dart
</read_first>

<action>
Run: `flutter test`

All tests must pass. If any fail after the fixes, identify the cause and fix it.
</action>

<acceptance_criteria>
- `flutter test` exits with code 0
- Output contains `All tests passed!`
- No test regressions from the warning fixes
</acceptance_criteria>

---

### Task 7: Final flutter analyze check

<action>
Run: `flutter analyze --no-fatal-infos`

Review output. Target: 0 warnings (the `warning` level items). Infos (deprecated APIs, prefer_const) are acceptable if any remain after dart fix auto-fix — but all warnings must be resolved.
</action>

<acceptance_criteria>
- `flutter analyze --no-fatal-infos` exits with code 0
- Zero `error` level issues
- Zero `warning` level issues (unnecessary_non_null_assertion, dead_null_aware_expression removed)
- Remaining `info` level issues are acceptable (non-breaking)
</acceptance_criteria>

## Verification Criteria

```
must_haves:
  - dart fix --apply ran without crash
  - No unnecessary_non_null_assertion warnings remain
  - withOpacity usages replaced with withValues(alpha:)
  - activeColor replaced with activeThumbColor in Switch widgets
  - use_build_context_synchronously fixed in add_transaction_screen
  - dead_null_aware_expression fixed in admin_transactions_screen
  - flutter test passes (all 33 tests)
  - flutter analyze --no-fatal-infos exits 0
```
